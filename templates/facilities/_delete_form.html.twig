<form method="post" action="{{ path('app_facilities_delete', {'id': facility.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facility.id) }}">
    <button class="btn">Delete</button>
</form>
