{% extends 'base.html.twig' %}

{% block title %}Facilities index{% endblock %}

{% block body %}
    <h1>Facilities index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for facility in facilities %}
            <tr>
                <td>{{ facility.id }}</td>
                <td>{{ facility.name }}</td>
                <td>
                    <a href="{{ path('app_facilities_show', {'id': facility.id}) }}">show</a>
                    <a href="{{ path('app_facilities_edit', {'id': facility.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_facilities_new') }}">Create new</a>
{% endblock %}
