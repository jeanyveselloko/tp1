{% extends 'base.html.twig' %}

{% block title %}AnnounceType{% endblock %}

{% block body %}
    <h1>AnnounceType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ announce_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ announce_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_announce_type_index') }}">back to list</a>

    <a href="{{ path('app_announce_type_edit', {'id': announce_type.id}) }}">edit</a>

    {{ include('announce_type/_delete_form.html.twig') }}
{% endblock %}
