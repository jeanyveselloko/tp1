{% extends 'base.html.twig' %}

{% block title %}AccomodationType{% endblock %}

{% block body %}
    <h1>AccomodationType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accomodation_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ accomodation_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accomodation_type_index') }}">back to list</a>

    <a href="{{ path('app_accomodation_type_edit', {'id': accomodation_type.id}) }}">edit</a>

    {{ include('accomodation_type/_delete_form.html.twig') }}
{% endblock %}
